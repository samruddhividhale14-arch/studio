{
  "entities": {
    "Farmer": {
      "title": "Farmer",
      "type": "object",
      "description": "Represents a farmer in the AgriSync application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the farmer, typically matching the user's UID."
        },
        "name": {
          "type": "string",
          "description": "The farmer's full name."
        },
        "email": {
          "type": "string",
          "description": "The farmer's email address.",
          "format": "email"
        },
        "language": {
          "type": "string",
          "description": "The farmer's preferred language."
        }
      },
      "required": ["id", "name", "email"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}/farmers/{farmerId}": {
      "schema": {
        "$ref": "#/entities/Farmer"
      },
      "description": "Stores farmer profiles. The `farmerId` should match the `userId`, enforcing a one-to-one relationship where a user owns their farmer profile."
    }
  }
}
